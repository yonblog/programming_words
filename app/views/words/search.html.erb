<%= form_with(url: search_words_path, local: true, method: :get, class: "search_form") do |form| %>
  <%= form.text_field :keyword, placeholder: "単語を検索する", class: "search_input" %>
  <%= form.submit "検索", class: "search_btn" %>
<% end %>

  <% @words.each do |word| %>
    <%= render partial: "word", locals: { word: word } %>
  <% end %>
</div>